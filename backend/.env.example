# Copy these into your shell, or prefix them to the start command.
# Cart layout to load: layperson or rn
CUEBOT_CART=layperson
# Port for the backend
PORT=8080
# Address of the frontend (used for Socket.IO CORS)
FRONTEND_ORIGIN=http://localhost:5173
